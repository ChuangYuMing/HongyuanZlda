import { NAME } from './constants'

export const LOG_OUT = `${NAME}/LOG_OUT`
export const UPDATE_FILTER_SETTING = `${NAME}/UPDATE_FILTER_SETTING`
export const UPDATE_TODAY_SYMBOL = `${NAME}/UPDATE_TODAY_SYMBOL`
export const SET_TODAY_SYMBOL = `${NAME}/SET_TODAY_SYMBOL`
