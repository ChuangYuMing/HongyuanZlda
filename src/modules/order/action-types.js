import { NAME } from './constants'

export const NEW = `${NAME}/NEW`
export const CHANGE_ORDER_STATUS = `${NAME}/CHANGE_ORDER_STATUS`
export const SHOW_ORDER_QUOTE = `${NAME}/SHOW_ORDER_QUOTE`
export const UPDATE_TICK = `${NAME}/UPDATE_TICK`
